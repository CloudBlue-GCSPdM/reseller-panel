import React from 'react';

const SubTitle = ({props}) => {

    
    return <div>
        
        <h2 class="title is-2">{props.subtitle}</h2>
        <br/>
    </div>;
};

export default SubTitle;
